{
  "name": "inbetween",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "InBetween core runtime foundation.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/**/*.test.js",
    "lifecycle": "npm run build && node dist/src/dev/run-lifecycle.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
